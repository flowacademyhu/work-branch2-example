// adding comment -gy

const sum = (a, b) => { // writen by Dani
  return a + b;
};
const sub = (a, b) => { // writen by Dani
  return a - b;
};

const mul = (a, b) => { // writen by gy
  return a * b;
};

const div = (a, b) => { // writen by gy
  return a / b;
};

module.exports = {
  sum,
  sub,
  mul,
  div
};
